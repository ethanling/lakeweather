import React from 'react'
import styled from 'styled-components'

const StyledWrapper = styled.div`
	
`

const DataDisplay = ({ direction, low, high }) => (
	<StyledWrapper>
		
	</StyledWrapper>
)

export default DataDisplay
