import styled from 'styled-components'

export const StyledWaveAlert = styled.div`
    opacity: ${props => props.show ? '1' : '0'};
`

export const StyledWindAlert = styled.div`
    opacity: ${props => props.show ? '1' : '0'};
`
